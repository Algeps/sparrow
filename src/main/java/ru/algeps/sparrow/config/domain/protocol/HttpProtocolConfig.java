package ru.algeps.sparrow.config.domain.protocol;

import ru.algeps.sparrow.config.Protocol;

public class HttpProtocolConfig extends ProtocolConfig {
  public HttpProtocolConfig(Protocol protocol) {
    super(protocol);
  }
}
